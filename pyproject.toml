[project]
name = "monarch-money-mcp"
version = "0.1.0"
description = "MCP server for MonarchMoney financial data access"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "certifi==2026.2.25",
    "mcp>=1.9.4",
    "monarchmoneycommunity>=0.1.15",
]

[project.scripts]
monarch-money-mcp = "server:main"
